package signals.requests
{
	import org.osflash.signals.Signal;
	
	public class RequestLanguageLoadSignal extends Signal
	{
		public function RequestLanguageLoadSignal(...valueClasses)
		{
			super(valueClasses);
		}
	}
}