package signals.requests
{
	import org.osflash.signals.Signal;
	
	public class RequestLanguageUpdateSignal extends Signal
	{
		public function RequestLanguageUpdateSignal(...valueClasses)
		{
			super(valueClasses);
		}
	}
}